---
name: memory-store-management
description: Inspect, categorize, and clean Hermes memory stores â€?USER.md, MEMORY.md, LTM, STM, and chat buffer â€?when the user asks about remembered data or wants cleanup.
---

# Memory Store Management

## Trigger
- User asks "what do you remember about me?"
- User asks "check what's in my profile/memory"
- User wants cleanup of old, incorrect, or sensitive entries
- User sees an unexpected memory-related system message (`ğŸ’¾ Self-improvement review: ...`) and wants to investigate
- User notices changes to their user profile or memory and wants to verify

## Inspection checklist â€?check ALL stores

### 1. USER.md (`/home/{{USERNAME}}/.hermes/memories/USER.md`)
User profile â€?identity, preferences, personal details. Read directly.

### 2. MEMORY.md (`/home/{{USERNAME}}/.hermes/memories/MEMORY.md`)
Personal notes â€?environment facts, project conventions, tool quirks. Read directly.

### 3. LTM â€?long_term_entries (SQLite)
Table in `/home/{{USERNAME}}/.hermes/memory_store.db`:
```python
cursor.execute("SELECT id, category, key, value, confidence, created_at FROM long_term_entries ORDER BY id;")
```
Key categories: `user_preferences`, `user_profile`, `qzone` (QQç©ºé—´ posts), `knowledge/topic_*` (auto-extracted topic fragments, usually garbage).

### 4. STM â€?short_term_entries (SQLite)
Session-level working memory. Can be purged freely.

### 5. chat_message_buffer (SQLite)
Full chat history archive. **Context passed to model is NOT the full buffer** â€?only last 5 minutes (30 lines, 1500 char cap) from in-memory buffer. The SQLite store is permanent reference for reply chains.

## Presentation to user
Categorize entries before showing:
- **Useful/keep** â€?active preferences, important facts, explicit requests
- **Maybe outdated** â€?old emotional moments, stale session summaries, roleplay artifacts from different interaction modes
- **Garbage** â€?low-confidence auto-extracted fragments (`knowledge/topic_*` at 0.2 confidence), random noise, orphaned entries

Ask for confirmation before deleting. Let the user decide what to keep.

### User cleanup flow (preferred)
1. User says "æ£€æŸ¥æ ¸å¿ƒæ–‡ä»? â†?check MEMORY.md first, not USER.md
2. User says "çœ‹é•¿æœŸè®°å¿? â†?dump LTM â†?they want the full raw list (id, category, key, value preview, confidence, created_at), not a summary
3. Categories they've explicitly flagged as garbage: `knowledge/topic_*` at 0.2 confidence, `decisions/decision` noise, orphaned `user_profile/user_identity` single-word values
4. User prefers to see raw data categorized â†?then say which IDs to delete â†?do not delete without confirmation
5. When user says "æ¸…ç†å¹²å‡€äº? or similar, VERIFY by re-checking each store and reporting the post-cleanup counts
6. User may reference entries by meaning ("åˆšåˆšè¿™ä¸ªé‡Œé¢çš?ï¼?8æ¸…ç†æ?) rather than formal cleanup scope â€?interpret flexibly
7. After any deletion batch, confirm the result by re-querying and showing the new count

## Cleanup methods by store

| Store | Method |
|-------|--------|
| USER.md | `memory(action="remove", target="user", old_text=...)` |
| MEMORY.md | `patch(old_string=..., new_string="")` on the file |
| LTM (long_term_entries) | `DELETE FROM long_term_entries WHERE id IN (...)` |
| STM (short_term_entries) | `DELETE FROM short_term_entries` |
| chat_message_buffer | `DELETE FROM chat_message_buffer WHERE created_at < now-300` (time-window cleanup â€?keep only last 5 min per user explicit preference) |
| USER.md bulk | `memory(action="remove", ...)` per entry |

## Known pitfalls
- **Intimate roleplay entries**: User has explicitly requested removal of intimate roleplay preference entries from both USER.md and MEMORY.md. Check both files.
- **Knowledge topic garbage**: `knowledge/topic_*` entries at 0.2 confidence are auto-generated by the system, not user input. Safe to bulk-delete.
- **Chat buffer has NO auto-cleanup**: The `trim_chat_buffer()` function exists in `store.py` but is never called from any production path. Buffer grows unbounded. User explicitly prefers **time-window cleanup** (delete all rows older than 5 minutes) over count-based trimming.
- **Background self-improvement review**: Every ~10 conversation turns, Hermes spawns a background agent fork that reviews the session for memory/skill saves. It emits `ğŸ’¾ Self-improvement review: ...` messages. If user sees this and asks about it, explain it's the background review system, not a separate process they need to manage.
- **qnibus entries**: QQç©ºé—´ sayings (`qzone/*`) and cron logs (`cron/*`) occupy most of LTM. These are normal and can stay.

## Reference files
- `references/hermes-memory-architecture.md` â€?detailed architecture notes (context window, memory stores, self-improvement review internals)
- `references/feishu-cli-setup.md` â€?how to install and configure the Feishu CLI (lark-cli), bind to Hermes, and handle auth login flow
